package com.android.systemui.screenshot.data.repository;

import android.app.IActivityTaskManager;
import com.android.systemui.screenshot.proxy.SystemUiProxy;
import kotlinx.coroutines.CoroutineDispatcher;

public final class DisplayContentRepositoryImpl {
    public DisplayContentRepositoryImpl(IActivityTaskManager iActivityTaskManager, SystemUiProxy systemUiProxy, CoroutineDispatcher coroutineDispatcher) {
    }
}
