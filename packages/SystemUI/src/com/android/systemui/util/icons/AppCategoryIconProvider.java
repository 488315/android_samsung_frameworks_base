package com.android.systemui.util.icons;

import kotlin.coroutines.Continuation;

/* loaded from: classes3.dex */
public interface AppCategoryIconProvider {
    Object assistantAppIcon(Continuation continuation);

    Object categoryAppIcon(String str, Continuation continuation);
}
