package com.android.systemui.statusbar.notification.stack.ui.viewmodel;

import javax.inject.Provider;

public final class HideListViewModel {
    public HideListViewModel(Provider provider) {
    }
}
