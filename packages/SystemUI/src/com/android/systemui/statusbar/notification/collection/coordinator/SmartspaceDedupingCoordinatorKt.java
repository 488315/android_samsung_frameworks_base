package com.android.systemui.statusbar.notification.collection.coordinator;

import java.util.concurrent.TimeUnit;

public final class SmartspaceDedupingCoordinatorKt {
    private static final long ALERT_WINDOW = TimeUnit.SECONDS.toMillis(30);
}
