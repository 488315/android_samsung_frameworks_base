package com.android.systemui.deviceconfig.domain.interactor;

import com.android.systemui.deviceconfig.data.repository.DeviceConfigRepository;

/* loaded from: classes2.dex */
public final class DeviceConfigInteractor {
    public final DeviceConfigRepository repository;

    public DeviceConfigInteractor(DeviceConfigRepository deviceConfigRepository) {
        this.repository = deviceConfigRepository;
    }
}
